# Hana Reader 后续路线

当前版本：`v1.6.0`（本地分支 `feat/reader-visual-simplification`）

本路线只记录真实边界和下一阶段工作。所有后续能力都必须保持本地优先、ResourceIO 安全边界和清爽的 Hana 蓝视觉体系。

## v1.2.0 已完成：视觉与交互收束

### 工作区

- 左侧文件树移除前端搜索、最近打开和多余说明，仅保留选择、刷新、打开目录和折叠。
- 文件夹使用黄色闭合 / 展开图标，常见文件类型使用轻量图标。
- 中间栏自适应宽度，阅读与编辑控件改为悬浮控件，左右留白统一。
- 标题收束为 h1 / h2 使用蓝色，h3-h6 使用正文色；代码和引用保留统一的明亮 Hana 蓝。
- 右侧只保留 AI 辅助 / 笔记本切换。

### 阅读、编辑与批注

- Markdown / 代码阅读、Milkdown 编辑、自动保存、冲突保护、安全 HTML 预览保持可用。
- 编辑态 `Ctrl/Cmd + Z` 交给原生编辑器；只读态按时间撤销最近一次批注或最近一次安全写回。
- 批注侧栏移除，选区操作改为正文下方悬浮菜单。
- 批注使用正文橙色波浪下划线与悬浮气泡；高亮、下划线和文本锚点恢复保持。
- 通过 `resource.open` 的 `mode: reveal` 打开当前目录的本地文件资源管理器。

### AI 与 Notebook

- AI 默认读取当前打开的文本文件，不再显示预设、上下文长度或选区上下文配置。
- Notebook 收束为选择、新建、改名、直接编辑和 Markdown 导出。
- 批注、Notebook、会话位置和 Copilot 历史仍只保存在当前浏览器本地。

## 已完成基础能力

### 阅读与编辑

- 选择本地文件夹并延迟展开目录树。
- 阅读 Markdown、JSON、HTML、JavaScript、TypeScript、Python 等文本文件。
- Markdown 安全渲染、GFM 表格、任务列表、代码高亮和安全 HTML 预览。
- 本地 Milkdown Markdown 所见即所得编辑器与多类型源码编辑器。
- 多类型文本安全写回、版本冲突检测、自动保存失败处理和最近一次写回撤销。
- 单文件 2 MB 读取上限、编辑内容 512 KB 上限。

### AI Copilot

- 通过插件 route 调用 Hana `model:sample-text`。
- 当前文件与对话历史按受限字段提交，模型不可用时本地阅读、编辑和 Notebook 仍可用。
- 支持直接提问、失败重试和按文件保存对话历史。

### 受限服务端搜索

- `POST /resources/search` 仍保留递归文本扫描能力，供未来重新接入受控搜索体验。
- 服务端边界：最多 500 个文件、单文件 1 MB、总读取 8 MB、最多 100 个结果。
- 本轮不在前端提供搜索入口，也不做常驻索引。

## v1.2.1 修复

- 修复左侧文件树折叠后无法重新展开的问题；折叠态现在显示独立的展开按钮。
- 版本修订号同步更新，避免旧 iframe / 静态资源缓存继续加载旧布局。

## v1.3.0 清爽界面细节

- 移除左侧重复根目录行和底部状态栏，压缩文件树行距并统一字号。
- 统一灰色滚动条；标题、列表标记、表头和代码块改为中性黑 / 白灰配色。
- 增加批注擦除、选区菜单自动消失、Enter 保存批注和 Shift + Enter 换行。
- Copilot 改为 Enter 发送、Shift + Enter 换行，并收束为轻量对话气泡。
- Notebook 支持 Markdown 导出与右键删除。

## v1.3.1 修复

- 修复批注气泡因浏览器选区丢失而无法打开的问题。
- 禁用下划线避让字母下行部的行为，避免 `g` 等字母造成断线。
- 修复 Notebook 导出触发方式，并保持右键删除入口可用。

## v1.3.2 修复

- 左侧文件树统一为较大字号与更紧凑行距，避免不同文件类型视觉不一致。
- 中间阅读 / 编辑区改为浅灰背景。
- 批注恢复为带引用、取消、保存操作的完整缩略气泡。
- Notebook 删除改为右键 / 删除按钮后显示明确确认提示，导出改走 ResourceIO 选择已有文本文件并安全覆盖。

## v1.3.3 界面与编辑同步

- 左侧文件树字号略微增大；同级母文件夹增加分组间距，组内内容保持紧凑。
- 两侧栏改为浅灰，中间阅读 / 编辑区改为近白色。
- 停止新增批注，仅保留高亮、下划线与擦除。
- 编辑 / 只读保持统一宽度，并同步切换前后的滚动位置。
- Notebook 删除确认改为浮层，不额外占用一整行。

## v1.3.4 视觉微调

- 两侧栏进一步降低明度，文件树字号明显增大并强化母文件夹分组间距。
- Notebook 删除确认改为纯白 / 深灰实底浮框，避免透明背景导致文字不可读。

## v1.3.5 文件树与滚动条

- 文件树行距约扩大一倍，保留当前较大字号；同级母文件夹保持更明显分组。
- 两侧栏灰度进一步加深。
- 三栏滚动条默认隐藏，仅在对应区域滚动时短暂显示。
- 非文件夹图标统一为中性灰，黄色仅保留给文件夹图标。

## v1.3.6 文件栏与侧栏色彩

- 左侧顶部增加中等字号的蓝色“文件栏”标题。
- Markdown 文件图标使用独立蓝色，其余非文件夹图标保持中性灰。
- 两侧栏灰度进一步加深。

## v1.3.7 代码阅读样式

- 修正 README / Markdown 文件图标的蓝色状态。
- 源代码、JSON、消息类文本不再使用灰色代码卡片与蓝色竖线，改为普通正文式展示。
- 三栏灰度对比继续保持侧栏更深、中间更白。

## v1.3.8 JSON 与三栏灰度修复

- JSON 语法高亮改为以黑色为主，仅保留必要层次。
- 修复侧栏背景被后续级联规则覆盖的问题，确保两侧栏真正使用更深灰色。

## v1.3.9 米白侧栏与笔记本重构

- 两侧栏改为淡米白 / 米黄色，与纯白阅读区形成轻微但清晰的区分。
- 笔记本改为轻量纸张感：柔和标签、清晰标题和白色编辑纸面。

## v1.4.0 侧栏配色收敛

- 两侧栏从偏黄的米色收敛为极浅的中性米白，不再使用纸张质感或黄色氛围。
- 笔记本保留清晰结构，但改用白色编辑面、浅灰边界和轻量控件，避免过度设计。

## v1.4.1 Markdown 代码块与笔记本工具栏

- Markdown fenced code block 复用引用块的安静边框与浅色背景，保留语法高亮。
- 笔记本“导出 / 删除”按钮固定在同一行。

## v1.4.2 标题拉丁字符统一

- 标题中的英文、数字与序号统一继承标题本身的 Georgia / Noto Serif SC 字体。
- 取消序号单独使用无衬线字体造成的割裂感。

## v1.4.3 Quiet 读写旋钮集成

- 中间栏原有的读写按钮替换为 `assets/native-knob.svg`。
- 左侧为只读、右侧为编辑；点击和左右方向键可操作。
- 先完成可中断的 SVG 转场，再切换编辑器；reduced-motion 下直接切换。
- 实验源目录保持独立，不参与 Reader 构建。

## v1.4.4 Quiet 读写旋钮稳定性修复

- 只读 / 编辑两种状态共用阅读区右上角的绝对定位锚点。
- 旋钮缩小到原方案约三分之一，并移除编辑模式状态文案。
- 渲染更新时复用已加载的 SVG object，减少切换文件和模式时的闪烁。

## v1.5.0 插画化工作台与稳定阅读底座

- 中间栏旋钮升级为独立于文件内容的持久 dock；切换文件时复用整个控件节点。
- 接入初始阅读插画、AI 空状态蝴蝶、文件栏插画标题、两侧收起图标与下半部海浪动效。
- 收紧选区工具条：使用“划线”、字号加大、硬边矩形。

## v1.5.1 矢量化视觉与平滑折叠修正

- 折叠图标改为内联 SVG；展开位置固定在窄条顶部。
- 旋钮挂载点与工作区壳层保持独立，侧栏宽度使用持久 grid 的 280ms transition。

## v1.5.2 透明插画与安静收起栏

- 中间空状态改用透明抠图 PNG，避免原图背景色与中间栏冲突，也避免大幅插画路径化后的失真。
- 收起栏删除大面积海浪与连续动画，只保留每侧 3 个独立小浪花；右侧水平镜像，位置轻微错落。
- 旋钮缩小约 5%，右侧栏拖拽和收起 / 展开时通过持久 dock 跟随移动。

## v1.5.3 放大空状态与冲浪收起栏

- 初始阅读插画放大约 40%，让空状态在中间阅读区承担更明确的视觉主体。
- 小浪花换为定制 SVG 卷浪：加入泡沫、浪峰和飞溅水珠，减少旧 PNG 在小尺寸下的模糊与单薄感。
- 三朵浪花分布到上、中、下三个高度，略微越过窄 rail 边界；收起时从外侧错落拍入，静止后不持续运动。

## v1.5.4 加宽折叠侧栏

- 左右折叠 rail 从 38px 调整为 64px，减少按钮与浪花在窄栏中的拥挤感。
- 重新校准展开按钮和三朵浪花的落点，让加宽后的 rail 仍保持冲浪方向和适度越界。
- 工作区 grid、旋钮 dock、窄屏布局和版本缓存同步更新。

## v1.6.0 大幅海浪与阅读留白

- 将用户提供的透明海浪图裁成紧凑纵向素材，作为两侧完整高度的装饰图层；右侧使用镜像，不重复堆叠小图。
- 海浪越过 64px rail 进入中间阅读区，折叠时从外侧涌入，使用 transform / opacity 动效并支持 reduced-motion。
- 只读内容左右 padding 从 `clamp(22px, 4vw, 64px)` 调整为 `clamp(30px, 5.8vw, 96px)`；编辑内容不跟随扩大。

## 下一阶段：优先级

1. **批注导入 / 导出**：继续使用 sidecar 或本地 JSON，不改变原始 Markdown。
2. **Notebook 本地数据维护**：完整 JSON 导出、导入预览、存储损坏恢复和清理入口。
3. **轻量阅读历史**：多标签或固定阅读项，复用当前单编辑器生命周期，不引入多 Milkdown 实例。
4. **长文档体验**：代码分段渲染、Markdown 分段处理，先建立基准文件和 Chromium 性能回归。
5. **仓库维护**：清理死 CSS，补充构建产物体积与依赖审计；旋钮实验源目录仍不参与 Reader 构建。
6. **搜索重新接入**：只有在确定交互入口和用户需求后复用现有 route，不重复实现后台索引。

## 需要 Hana 宿主或协议设计的能力

- 跨文件问答：需要明确检索范围、模型授权和上下文计费边界。
- Git 状态与 Diff：需要稳定的宿主读取或执行能力，浏览器不能直接访问本地 `.git`。
- 批注跨设备同步：需要 ResourceIO sidecar 或专门的审阅文件格式，并定义版本、冲突和隐私边界。
- Notebook 创建新文件：取决于宿主是否提供安全的资源创建 API。
- 更完整的 `resource.open` 语义：需由 Hana 宿主决定 reveal / open / external 等模式和平台兼容行为。

## 旋钮实验集成边界

`knob-motion-lab/` 仍是独立的旋钮实验源目录，不参与 Reader 构建、安装包或测试。当前 Reader 仅接入复制后的 `assets/native-knob.svg`，由 `src/panel.js` 在中间栏读写控件位置加载；后续若更新实验方案，应先同步检查插件内复制资产和读写状态逻辑。

## 兼容与安全边界

- 浏览器不直接读取本地路径，用户资源继续通过 `ctx.resources` / ResourceIO 路由访问。
- 写回必须携带读取时的 `version` 和 SHA-256，不接受无基线覆盖。
- Copilot route 对文件、选区、历史条数和长度做服务端限制。
- 批注和 Notebook 默认保存在浏览器本地，不提交 API Key、Cookie、个人文件、会话导出或真实项目内容。
- 每次发布前运行 `npm test`，确认版本、构建产物、路由和静态结构回归全部通过。
