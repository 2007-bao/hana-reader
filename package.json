{
  "name": "hana-reader",
  "version": "0.8.5",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/panel.js --bundle --minify --format=esm --target=es2020 --outfile=assets/panel.js",
    "test": "npm run build && node --test --test-concurrency=1"
  },
  "dependencies": {
    "@milkdown/kit": "^7.22.1",
    "dompurify": "^3.4.14",
    "highlight.js": "^11.12.0",
    "markdown-it": "^15.0.1",
    "markdown-it-task-lists": "^2.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
